1. compile commands
		javac -d ./bin ./src/benchmarks/tsp/*.java

		note: We have compiled it in advance, you can use it directly.
2. run commands
		cd bin
		java benchmarks.tsp.Tsp ../src/tspfiles/map16c 5